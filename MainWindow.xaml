<Window x:Class="Personal_Finance_Tracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Personal_Finance_Tracker"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Naslov -->
        <TextBlock Grid.Row="0"
               Grid.ColumnSpan="3"
               Text="Personal Finance Tracker"
               HorizontalAlignment="Center"
               Margin="0,20,0,20"
               FontSize="18"
               FontWeight="Bold"/>

        <!-- Stanje racuna -->
        <Label Grid.Row="1"
           Grid.Column="1"
           Content="Stanje računa:"
           HorizontalAlignment="Left"/>

        <Label Grid.Row="1"
           Grid.Column="1"
           Content="0.00 RSD"
           x:Name="lblIznos"
           HorizontalAlignment="Right"/>

        <!-- Dugmad -->
        <Button Grid.Row="2"
            Grid.Column="0"
            x:Name="btnDodaj"
            Content="DODAJ"
            Margin="10" Click="btnDodaj_Click"/>

        <Button Grid.Row="2"
            Grid.Column="1"
            x:Name="btnIzmeni"
            Content="IZMENI"
            Margin="10" Click="btnIzmeni_Click"/>

        <Button Grid.Row="2"
            Grid.Column="2"
            x:Name="btnObrisi"
            Content="OBRIŠI"
            Margin="10" Click="btnObrisi_Click"/>

        <!-- Tabela -->
        <DataGrid Grid.Row="3"
              Grid.ColumnSpan="3"
              x:Name="dgTransakcije"
              Margin="10"
              ColumnWidth="*"/>

    </Grid>
</Window>
